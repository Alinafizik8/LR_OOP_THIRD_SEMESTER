spring:
  application:
    name: lr-oop-spring

  datasource:
    url: jdbc:postgresql://localhost:5433/lr_oop_db
    username: lr_user
    password: lr_pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate  # или create-drop для тестов
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  jackson:
    default-property-inclusion: non_null

logging:
  level:
    com.example.lr: DEBUG
    org.springframework.jdbc: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"


#spring:
#  application:
#    name: lr-oop-spring
#
#  datasource:
#    url: jdbc:postgresql://localhost:5432/lr_oop_db
#    username: lr_user
#    password: lr_pass
#    driver-class-name: org.postgresql.Driver
#
#  sql:
#    init:
#      mode: always  # ????????? schema.sql ??? ??????
#      # schema-locations: classpath:schema.sql  # ?? ?????????
#
#  jackson:
#    default-property-inclusion: non_null
#
#logging:
#  level:
#    com.example.lr: DEBUG
#    org.springframework.jdbc: DEBUG  # ?????????? SQL (???????????)
#  pattern:
#    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
