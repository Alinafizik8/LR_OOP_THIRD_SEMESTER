server:
  port: 8080
  servlet:
    context-path: /api  # Это добавит префикс /api ко всем endpoint'ам

spring:
  application:
    name: lr-oop-spring

  datasource:
    url: jdbc:postgresql://localhost:5432/lr_oop_db
    username: lr_user
    password: lr_pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  security:
    user:
      name: admin
      password: lr_secure_password321
      roles: ADMIN, USER

  jackson:
    default-property-inclusion: non_null

# Actuator — включаем эндпоинты
management:
  endpoints:
    web:
      exposure:
        include: health,info,env,beans,metrics
      base-path: /management  # Опционально
  endpoint:
    health:
      enabled: true
      show-details: always
    info:
      enabled: true

# CORS configuration для фронтенда
cors:
  allowed-origins: http://localhost:3000
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

logging:
  level:
    com.example.lr: DEBUG
    org.springframework.jdbc: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web: DEBUG  # Добавь для отладки web запросов
    org.springframework.security: DEBUG  # Добавь для отладки security
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"